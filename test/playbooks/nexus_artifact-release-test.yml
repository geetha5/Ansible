---

- name: Nexus Artifact Release test
  hosts: localhost
  become: yes
  become_user: root
  vars_files:
        - /vagrant/test/vars/nexus_artifact-release-test.yml

  roles:
    - base
    - nexus_artifact